ВСЕ ЧТО ТУТ НАПИСАНО - НАПИСАНО НЕЙРОНКОЙ В ТОМ ЧИСЛЕ И ЭТОТ ТАК НАЗЫВАЕМЫЙ КАЛЛ

# Minecraft Skin Pixelart Converter

Программа для преобразования скинов Minecraft (64x64 пикселей) в пиксель-арт из блоков (1024x1024 пикселей) и плащей Minecraft (64x32 пикселей) в пиксель-арт (1024x512 пикселей).

## Установка

```bash
pip install -r requirements.txt
```

## Структура проекта

```
minecraft-skin-pixelart/
├── minecraft_skin_pixelart/    # Основной пакет
│   ├── __init__.py
│   ├── block_palette.py        # Управление текстурами блоков
│   ├── color_matcher.py        # Поиск ближайшего цвета
│   ├── skin_processor.py       # Обработка скинов
│   ├── cape_processor.py       # Обработка плащей
│   └── exceptions.py           # Пользовательские исключения
├── tests/                      # Тесты
│   ├── __init__.py
│   ├── test_block_palette.py
│   ├── test_color_matcher.py
│   └── test_skin_processor.py
├── block/                      # Директория с текстурами блоков
├── requirements.txt            # Зависимости
├── pytest.ini                  # Конфигурация pytest
└── README.md                   # Этот файл
```

## Использование

### Графическое меню (GUI) - самый простой способ

Запустите простое графическое меню:
```bash
python gui_menu.pyw
```

Или дважды кликните на файл `run_gui.bat` (Windows).

**Возможности GUI:**
- 📁 Загрузка скина (64x64 или 64x32 пикселей)
- 🎨 Загрузка плаща (64x32 или 22x17 пикселей)
- 👁️ Превью загруженных файлов
- ⚡ Обработка обоих файлов одновременно
- 💾 Автоматическое сохранение результатов

### Через командную строку

```bash
# Конвертация скина (64x64 → 1024x1024)
python -m minecraft_skin_pixelart --input my_skin.png --output result.png

# Конвертация плаща (64x32 → 1024x512)
python -m minecraft_skin_pixelart --input my_cape.png --output result.png --cape

# Короткая форма
python -m minecraft_skin_pixelart -i skin.png -o output.png
python -m minecraft_skin_pixelart -i cape.png -o output.png -c

# С автоматическим именем выходного файла
python -m minecraft_skin_pixelart -i skin.png
python -m minecraft_skin_pixelart -i cape.png --cape

# Указание пользовательской директории с текстурами блоков
python -m minecraft_skin_pixelart -i skin.png -o output.png --blocks-dir path/to/blocks
```

**Параметры:**
- `--input, -i` (обязательный) - путь к входному файлу (скин: 64x64, плащ: 64x32)
- `--output, -o` (опциональный) - путь к выходному файлу (по умолчанию: minecraft_skin_output.png или minecraft_cape_output.png)
- `--cape, -c` (опциональный) - обработать как плащ (64x32) вместо скина (64x64)
- `--blocks-dir, -b` (опциональный) - путь к директории с текстурами блоков (по умолчанию: block)

### Через Python API

```python
from minecraft_skin_pixelart import BlockPalette, ColorMatcher, SkinProcessor, CapeProcessor

# Инициализация компонентов
palette = BlockPalette("block")
palette.load_blocks()

matcher = ColorMatcher(palette)

# Конвертация скина
skin_processor = SkinProcessor(palette, matcher)
skin_processor.convert_skin("input_skin.png", "output_skin.png")

# Конвертация плаща
cape_processor = CapeProcessor(palette, matcher)
cape_processor.convert_cape("input_cape.png", "output_cape.png")
```

## Тестирование

```bash
# Запуск всех тестов
pytest

# Запуск с покрытием
pytest --cov=minecraft_skin_pixelart --cov-report=html

# Запуск property-based тестов
pytest -k property
```

## Требования

- Python 3.8+
- Pillow >= 10.0.0
- hypothesis >= 6.0.0
- pytest >= 7.0.0
- pytest-cov >= 4.0.0

## Требования к входным файлам

### Скины
- **Формат:** PNG изображение размером **64x64 пикселей**
- **Выходной файл:** PNG изображение размером **1024x1024 пикселей**

### Плащи
- **Формат:** PNG изображение размером **64x32 пикселей**
- **Выходной файл:** PNG изображение размером **1024x512 пикселей**

### Текстуры блоков
- PNG файлы в директории `block/` (по умолчанию)

## Примеры использования

### Пример 1: Конвертация скина
```bash
python -m minecraft_skin_pixelart -i steve_skin.png -o steve_pixelart.png
```

### Пример 2: Конвертация плаща
```bash
python -m minecraft_skin_pixelart -i my_cape.png -o cape_pixelart.png --cape
```

### Пример 3: Использование с автоматическим именем
```bash
python -m minecraft_skin_pixelart -i alex_skin.png
# Создаст файл minecraft_skin_output.png

python -m minecraft_skin_pixelart -i cape.png -c
# Создаст файл minecraft_cape_output.png
```

### Пример 4: Пользовательская директория блоков
```bash
python -m minecraft_skin_pixelart -i skin.png -o result.png -b custom_blocks/
```

## Что делает программа?

### Для скинов (64x64 → 1024x1024):
1. **Загружает текстуры блоков** из указанной директории
2. **Анализирует цвета** каждого блока
3. **Читает входной скин** (64x64 пикселей)
4. **Для каждого пикселя скина** находит наиболее подходящий блок по цвету
5. **Создает выходное изображение** (1024x1024 пикселей), где каждый пиксель скина заменен на текстуру блока 16x16

### Для плащей (64x32 → 1024x512):
1. **Загружает текстуры блоков** из указанной директории
2. **Анализирует цвета** каждого блока
3. **Читает входной плащ** (64x32 пикселей)
4. **Для каждого пикселя плаща** находит наиболее подходящий блок по цвету
5. **Создает выходное изображение** (1024x512 пикселей), где каждый пиксель плаща заменен на текстуру блока 16x16

## Обработка ошибок

Программа корректно обрабатывает следующие ошибки:
- Неверный размер входного изображения (не 64x64 для скинов или не 64x32 для плащей)
- Отсутствие входного файла
- Отсутствие директории с блоками
- Пустая палитра блоков
- Ошибки сохранения выходного файла
